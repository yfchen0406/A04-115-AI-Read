# 11本 AI 書籍 × 12週工作流學習地圖

直接雙擊 `index.html` 即可離線使用。

## 網頁順序
1. Hero 主視覺：`images/hero-banner.webp`
2. 真正可點擊的 HTML 12 分頁導覽列
3. 各分頁 16:9 資訊圖：`images/infographic-01.webp` ～ `infographic-12.webp`
4. 各分頁詳細文字內容

## 替換 Hero
把新的橫幅圖轉成 WebP，命名為 `hero-banner.webp`，直接覆蓋 `images/hero-banner.webp`。
請勿在 Hero 圖片本身畫 01～12 分頁按鈕；真正按鈕由 HTML 產生。

## 替換 12 張資訊圖
依序命名為 `infographic-01.webp` ～ `infographic-12.webp`，覆蓋 images 內同名檔案即可，不需修改 HTML。

`12頁資訊圖表生成提示詞.md` 已保留，供後續逐張生成。
