# 《隨他們去》三版本動態閱讀統整入口

這個首頁統整目前最後完成的三個 V2 動態閱讀架構版本：

1. V2 原始架構版
2. V3 多啦A夢版
3. V4 Totoro 版

首頁提供簡介與版本卡片，按下「進入此版本閱讀」即可開啟完整的封面、旁白、字幕、章節控制與圖片動畫網頁。

`web-upload-package` 是自包含上傳包，三個版本放在 `versions/` 內，圖片全部使用 WebP。
